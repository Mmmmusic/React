import React,{Component} from 'react';
import writer from '../home.css';

class Writer extends Component{
  render(){
    return(
      <div className={writer.writerWrapper}>writerWrapper</div>
    )
  }
}

export default Writer;